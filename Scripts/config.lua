local ATM_config = {

    -- If false will unlock all tech regardless of level
    -- If true will only unlock tech up to your current level in game (updates when you level up)
    onlyUnlockUpToCurrentLevel = false,

    -- Delays loading technologies by 10 seconds (10000) default
    delayToAttemptToLoadTechnologies = 10000,

}

return ATM_config